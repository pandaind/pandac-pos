# Newman Test Reports

This directory contains test reports generated by Newman.

Reports are generated in multiple formats:
- **HTML**: Interactive report with detailed test results
- **JSON**: Machine-readable results for further processing
- **XML**: JUnit format for CI/CD integration

## Report Files

- `report-YYYYMMDD-HHMMSS.html` - HTML test report
- `report-YYYYMMDD-HHMMSS.json` - JSON test results
- `report-YYYYMMDD-HHMMSS.xml` - JUnit XML results (if configured)

## Viewing Reports

Open the HTML report in your browser:

```bash
# On macOS
open report-*.html

# On Linux
xdg-open report-*.html

# On Windows
start report-*.html
```

## Cleaning Old Reports

To remove old reports:

```bash
# Remove all reports
rm -rf newman-reports/*

# Keep only the latest 5 reports
ls -t newman-reports/*.html | tail -n +6 | xargs rm -f
```

## Note

This directory is included in `.gitignore` and should not be committed to version control.
